(function() {
  // Utiliser l'anglais...

  let theList = []; // Mauvais

  let productList = []; // Peut mieux faire

  let productCatalog = []; // Voilà !!!

  // Utiliser des noms pour les variables, propriétés et paramètres

  let index = 0;
  let currentUser = { id: 1, name: "Nico" };
  let price = 450;

  // Utiliser des verbes pour les fonctions et méthodes

  function getProduct(id) {
    let product = "WC";
    return product;
  }

  function getProducts() {
    let products = ["WC", "Toilet paper", "Soap"];
    return products;
  }

  function addProduct(product) {
    // Save this product in your amazing database
    return true;
  }
  // Utiliser 'is' pour les fonctions qui retournent 'toujours'
  // des valeurs booléennes

  function isReadable(book) {
    return true | false;
  }

  // Prononçable!!!

  function recdptrl() {
    // WTF?? : Received patrol? or Record department role?
  }

  // Conventions de nommage. Choisissez-en une et tenez-vous y.

  let camelCase = 1;
  let lower_case_with_underscore = 1;
  let PascalCase = 1;
})();
